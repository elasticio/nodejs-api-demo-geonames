var ExecutionParameters = require('./params.js').ExecutionParameters;
var messages = require('./messages.js');
var Executor = require('./executor.js').Executor;

var params = new ExecutionParameters(messages.newEmptyMessage())
                    .configuration({})
                    .snapshot({});

var component = null;

new Executor(component).execute(params);

